# Copy to .env and fill real values. .env is gitignored — never commit secrets.
MLFLOW_TRACKING_URI=http://localhost:5000
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
ECR_REPOSITORY=claims-ml
MODEL_STAGE=Production
